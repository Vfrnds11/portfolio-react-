# portfolio-react-
Portfólio profissional em React — páginas, componentes reutilizáveis, deploy
instruções de instalação/execução, mapa do site (páginas), seção “Como contribuir

{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
